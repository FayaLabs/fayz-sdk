# LOG — append-only iteration history

Format: `- #N · <app> · <task> · <result> · <commit> · <files/notes>`

- #1 · sprint-setup · scaffold sprint (README, CADENCE, PLAN, DATA-MODEL, STATE, LOG) + resolve archetype model · done · (pending commit) · docs/dogfood-sprint/* — three-ring data model decided; beautyplace 112 tables map onto rings with zero Ring-0 changes. Next: B2.
- #2 · beauty-saas · B2 · done (typecheck pass) · 495d558 · src/config/dashboard.tsx — wired 7/10 dashboard metrics to real fayz.data (revenue-week, active-clients, avg-ticket, no-show-rate, new-clients-month, retention-rate, revenue-per-professional) over v_bookings/v_clients/v_staff; added week/month range + sumColumn helpers (no server aggregate in fayz.data). avg-rating/occupancy/product-sales left hardcoded w/ TODO(B4). Next: B3.
- #3 · beauty-saas · B3 · done (typecheck pass) · cee0b4c · src/config/dashboard.tsx — replaced 4 onboarding `check: async () => false` stubs with real existence queries via a `tableHasRows()` helper (countRows + `schema` param, try/catch→false). clients→v_clients, services→saas_core.services, schedule→saas_core.schedules, payments→public.payment_methods. No new view needed (schema param handles cross-schema). Next: B4.
- #5 · beauty-saas · B5 · done (typecheck pass EXIT=0, capability gate EXIT=0) · aeaaacb · plugins/plugin-agenda/src/{financial-bridge.ts,bridges/create-financial-bridge.ts,index.ts} — commission compute wired into AgendaFinancialBridge: new optional `createCommissionMovement(orderId, {dueDate?})` resolves professional via `orders.assignee_id`, reads `v_staff.commission_rate`, inserts payable `financial_movements` row (direction=debit, movement_kind=commission); idempotent per order. Pure `computeCommissionAmount(total, rate%)` exported for reuse/tests. No fin_commission_rules table — rate from staff_members.commission_rate (B4). SDK code committed to fayz-sdk current branch (main) per task instruction, matching prior iterations. Next: B6.
- #4 · beauty-saas · B4 · done (typecheck pass, EXIT=0; no TS touched) · 7f2c6d9 · supabase/migrations/20260616000001_staff_commission_rate.sql — verified B2/B3 wired metrics already have backing views (no new view needed); only gap was staff_members.commission_rate (absent from all 34 migrations). Authored idempotent migration: ADD COLUMN IF NOT EXISTS commission_rate numeric(5,2) NOT NULL DEFAULT 0 + CREATE OR REPLACE v_staff to surface it (security_invoker reasserted, grant). Staged not applied → B-CHECK. Source views for 3 hardcoded metrics (avg-rating/occupancy/product-sales) deferred until wired. Next: B5.
- #7 · beauty-saas · B7 · done (typecheck pass EXIT=0) · b6e0733 · beauty-saas/src/lib/booking.ts + src/pages/PublicBooking.tsx — confirmed tenant wiring end-to-end: setActiveTenantId fires on org switch via SDK org store setCurrentOrg (OrgInitializer + OrgSwitcher), plugin entityDefs tenant-scope through resolveDataProvider's `() => getActiveTenantId()` (supabase/archetype/fayz-api + tenant-keyed cache), real-vs-mock driven by VITE_SUPABASE_URL. Fixed the one gap: custom public-booking reads (listBookableServices/listProfessionals/getAvailableSlots) weren't tenant-scoped (relied on authed RLS context the anon flow lacks) → now filter tenant_id explicitly (services/v_staff/v_bookings all expose it); PublicBooking threads tenantId + gates loads on it. **beauty-saas code queue complete** → M-BEAUTY staged behind B-CHECK (human: apply migration + set env + login smoke + org-switch isolation test). FOCUS → pulse-store P1.
- #6 · beauty-saas · B6 · done (typecheck pass EXIT=0, build pass vite ✓) · 89677ad · beauty-saas/src/lib/booking.ts + src/pages/PublicBooking.tsx + src/config/pages.tsx — public booking flow ported (scoped) from beautyplace onto the three-ring model. booking.ts engine on fayz.data: listBookableServices (saas_core.services), listProfessionals (v_staff), pure generateDaySlots/filterAvailableSlots + getAvailableSlots (v_bookings conflicts + minAdvanceHours), createBooking writing order→booking→booking_items+order_items into saas_core (mirrors agenda provider insert shape; tenant_id from BookingDraft). Multi-step PublicBooking page (service→pro→date→time→client→success) at /book; tenant from ?tenant= / VITE_DEFAULT_TENANT_ID. Page sits behind app auth (clients.create) — anon hosting + person lookup/create-by-phone deferred to B-CHECK. Next: B7.
- #8 · pulse-store · P1 · done (typecheck pass EXIT=0) · 63f4950 · pulse-store/src/config/seed.ts — authored catalog→shop_* seed module. seedPulseCatalog({client?,tenantId?}) idempotently upserts pulseCatalog into shop_categories/shop_products/shop_product_images/shop_discounts (columns mirror SupabaseShopProvider); idempotency by tenant-scoped natural key (category.slug/product.sku/discount.code/image.url) so re-runnable; returns per-table touch counts; minimal structural SeedClient type (no @supabase/supabase-js hard dep); defaults client→getSupabaseClientOptional(), tenantId→PUBLIC_FAYZ_STORE_ID. **DISCOVERY:** pulse storefront uses createFayzShopProvider (legacy supabase mode) which is READ-ONLY (writes throw 501, anon key) and reads BARE tables (products/categories/...) with client-side placeOrder — NOT shop_* nor shop_place_order RPC. Seed targets canonical shop_* per task title + DATA-MODEL. Logged PULSE PROVIDER/SCHEMA NOTE in STATE: reconcile (prefer switching pulse to SupabaseShopProvider) before P3 e2e. Next: P2.
- #9 · pulse-store · P2 · done (typecheck EXIT=0, build pass vite ✓) · 4f325de · pulse-store/src/config/content.ts (new) + src/config/app.ts — extracted storefront content (announcement, hero slides, benefits, banners, newsletter copy, footer) out of hardcoded app.ts into src/config/content.ts as `pulseContent: StorefrontContent` (typed against storefront HomeConfig/FooterConfig). app.ts now consumes pulseContent.{announcement,home,footer} verbatim — one content-authoring surface. Category copy (name/description) already flows catalog.ts → seed.ts → shop_categories. NOTE: hero/banner/announcement are storefront-chrome with no shop_* table; data-driven home (content/settings table + seed-write path) deferred — needs migration + RLS, fold into P3 reconcile or P2b. **pulse-store code queue complete (P1+P2)** → M-PULSE staged behind P3 (human: reconcile provider/schema, run seed with write client, set env, place test order via shop_place_order RPC + RLS verify). FOCUS → resto-saas R1.
- #10 · resto-saas · R1 · done (typecheck pass EXIT=0) · 95b4aff · resto-saas/src/config/provider-env.ts (new) + menu.ts/orders.ts/tables.ts + .env.example — real provider selection. New shared provider-env.ts (`shouldUseFayzProvider(scope)`, `fayzClientEnv()`, `env()`) replaces the per-file duplicated `env`/`shouldUseFayz*Provider`/client-option plumbing in menu/orders/tables. Precedence: per-scope flag VITE_FAYZ_<SCOPE>_PROVIDER → global VITE_FAYZ_PROVIDER → auto-detect (runtime token present). menu/orders/tables now flip mock→real on a Fayz runtime token (were disabled-by-default behind explicit fayz/sdk flag). DISCOVERY: these 3 plugins ship ONLY fayz-runtime (createFayz*Provider via Fayz data API) + mock providers — no direct-Supabase menu/orders/tables provider; their real path is the runtime token, separate from SDK plugins (crm/financial/inventory) reading project Supabase via VITE_SUPABASE_URL. Completed .env.example (both backends + per-plugin overrides). Next: R2.
- #11 · resto-saas · R2 · done (typecheck pass EXIT=0) · 461adb2 · resto-saas/src/pages/MenuManager.tsx + src/data/mock-menu.ts (both deleted) — menu real data. The `/menu` surface is the **plugin** MenuPage (createRestoMenuPlugin→createMenuPlugin), already wired to createFayzMenuProvider by R1 (real fayz-runtime data; mock fallback only when no provider). MenuManager.tsx (hardcoded MOCK_CATEGORIES) + mock-menu.ts (mockMenu) were dead orphans — defined but never imported/routed (pages.tsx `/menu` is a nav-child pointer; the plugin owns the component). Removed both so the real-data plugin page is the single menu surface; kept types/menu-item.ts (MenuItem/productEntity consumed via types/index). Next: R3 (app-owned restaurant module/plugin seam — community-plugin template).
- #13 · resto-saas · R4 · done (typecheck EXIT=0 resto-saas + plugin-orders + plugin-tables; capability gate EXIT=0) · app 7895ea1 + sdk 99ea73d · plugins/plugin-{orders,tables}/src/data/fayz.ts (sdk) + resto-saas/src/restaurant/{orders,tables}.ts (app) — orders/tables real data + tenant scoping. Providers were already real (R1, Fayz-runtime); added optional `tenantId?: string | (() => string|undefined|null)` to createFayzOrdersProvider/createFayzTablesProvider: scoped reads prepend `tenant_id eq` filter and creates stamp `tenant_id` when supplied — backward-compatible no-op when omitted (runtime/RLS handles it). `order_items` deliberately untouched: saas_core.order_items has NO tenant_id column (scoped via parent order_id), confirmed in archetypes migration. restaurant/{orders,tables}.ts thread `tenantId: () => getActiveTenantId()` from @fayz-ai/core (cleared/reset on org switch, mirrors B7). Confirmed restaurant_tables RLS already present (20260101000006_project_tables.sql: full CRUD tenant policies). NOTE: resto RLS uses inline `tenant_id IN (SELECT tenant_id FROM saas_core.tenant_members WHERE user_id=auth.uid())`, not DATA-MODEL canonical `public.user_tenant_ids()` — flagged for M-LOCK. **resto-saas code queue complete (R1–R4)** → M-RESTO staged behind R-CHECK (human: provision DB + Fayz-runtime env + login smoke). FOCUS → agency-os A1.
- #12 · resto-saas · R3 · done (typecheck pass EXIT=0, build pass vite ✓) · 353ae1f · resto-saas: new src/restaurant/{index.ts,README.md} + moved provider-env/menu/orders/tables (config→restaurant) + module-toggles.tsx (merged ModuleToggles.tsx + registry.tsx's moduleTogglesPlugin); src/config/app.tsx + src/App.tsx — community-plugin seam. Consolidated all resto-specific plugin code into an app-owned `src/restaurant/` module. Single entry point `restaurantPlugins({orders}): PluginManifest[]` assembles [moduleToggles, menu, tables, orders] in nav order; callers pass only domain inputs (archetype lookups) — provider selection (shouldUseFayzProvider/fayzClientEnv), table/schema names, currency, nav placement all encapsulated. Each menu/orders/tables factory keeps the real-or-mock pattern (Fayz-runtime provider when token/flag set, else undefined → SDK plugin's bundled mock). app.tsx now `...restaurantPlugins(...)`. Removed dead orphans: pages/TableMap.tsx (hardcoded-mock page; tables PLUGIN owns /tables — parallel to R2's MenuManager removal) + registry.tsx (and its no-op `import './registry'` in App.tsx). README.md documents the seam contract + 3-step integration pattern for copying into a new community plugin. Next: R4 (orders/tables real data + RLS).
- #14 · agency-os · A1 · done (typecheck EXIT=0 no TS touched; SQL $$/DO blocks balanced; staged not applied) · fff33eb · agency-os/supabase/migrations/ (9 new files) — initial migration set. Created supabase/migrations/ (greenfield: agency-os had NO migrations). Copied the proven Ring-0 spine VERBATIM from beauty-saas (8 files: 20260101000001_saas_core [tenants/profiles/tenant_members/locations/plans/perms/RBAC/invitations/subs/invoices/notifications + create_tenant_with_owner + is_tenant_admin], 000002_project_rls [public.user_tenant_ids() + auto-discover RLS on public.* tenant_id tables], 000003_invitations_status, 000004_archetypes [persons/categories/products/services/orders/order_items/transactions/bookings/booking_items/schedules + RLS], 000005_locations_archetype, 000007_fix_archetype_rls [archetype RLS → saas_core.user_tenant_ids() SECURITY DEFINER], 000008_archetype_grants, 000010_grants) so all four apps share an IDENTICAL Ring 0 — the M-LOCK convention. Authored agency Ring-2 20260101000020_agency_subaccounts.sql (idempotent: CREATE IF NOT EXISTS + DROP POLICY IF EXISTS guards): public.snapshots (agency-owned GHL provisioning templates, tenant-scoped) + public.sub_accounts (1:1 extension of saas_core.tenants, PK=tenant_id; agency_tenant_id parent FK for the reseller hierarchy, snapshot_id FK, business_niche, timezone, white_label_domain, status CHECK active/paused/churned, onboarding_completed, metadata) — RLS keyed on saas_core.user_tenant_ids() + grants + updated_at triggers, per DATA-MODEL Ring 2. GHL mapping: sub-account = tenant (multiOrg/OrgSwitcher swaps it); agency hierarchy via sub_accounts.agency_tenant_id. NOTE: branch fay/dogfood-sprint carries 2 pre-existing uncommitted main edits (.env.example + config/app.tsx supabase publishable-key fallback) NOT part of A1 — left uncommitted, only supabase/migrations/ staged+committed. Next: A2 (replace Placeholder pages, wire conversations/CRM/calendars to real data).
- #15 · agency-os · A2 · done (typecheck EXIT=0 plugin-conversations + agency-os; capability gate EXIT=0; SQL $$ balanced 2 pairs; staged not applied) · app d121e6a + sdk fb3e687 · sdk: plugins/plugin-conversations/src/data/supabase.ts (new) + index.ts; app: agency-os/supabase/migrations/20260101000021_conversations.sql (new) + removed src/pages/Placeholder.tsx — wire conversations/CRM/calendars to real data. CRM + calendars are SDK plugins (createCrmPlugin/createAgendaPlugin) already reading project Supabase via resolveDataProvider + getActiveTenantId when VITE_SUPABASE_URL is set (B7 mechanism) — no change needed. Conversations shipped mock-only (M1 deferral) → built createSupabaseConversationsProvider reading/writing `conversations` + `conversation_messages` (listConversations w/ channel/status/search filters + tenant scope; getMessages ordered by at; sendMessage inserts outbound + rolls parent thread preview/timestamp/unread/reopen; markRead; setStatus); createSafeProvider now auto-selects it when getSupabaseClientOptional() returns a client, tenant-scoped via () => getActiveTenantId(), else mock. Migration 20260101000021_conversations.sql: both tables carry tenant_id FK→saas_core.tenants + RLS on saas_core.user_tenant_ids() (FOREACH DO block, mirrors A1 sub_accounts) + indexes + grants + conversations updated_at trigger (messages append-only). App already renders real plugins for conversations/calendars/CRM in app.tsx (no <Placeholder> mounted) — removed dead src/pages/Placeholder.tsx (createPlaceholder, zero refs). Pre-existing uncommitted .env.example + config/app.tsx edits (from A1) left untouched. Next: A3 (multi-org isolation — setActiveTenantId on org switch + RLS scoping across sub-accounts).
- #16 · agency-os · A3 · done (typecheck EXIT=0 no TS touched) · 0043899 · agency-os/docs/MULTI-ORG-ISOLATION.md (new) — multi-org isolation CONFIRMED, no code gap (B7 + A2 already wired the full chain). Verified end-to-end: (1) setActiveTenantId set on setCurrentOrg / cleared on reset (packages/saas/src/org/store.ts); (2) OrgSwitcher lists every sub-account tenant via createSupabaseOrgAdapter.listUserOrgs (reads saas_core.tenant_members for user_id, joins tenants) — each sub-account IS a saas_core.tenants row so all managed ones appear; (3) all agency plugins scope to active tenant: stock plugins (crm/agenda/financial/marketing/…) via resolveDataProvider + () => getActiveTenantId() (B7), conversations via createSafeProvider auto-select createSupabaseConversationsProvider({tenantId:()=>getActiveTenantId()}) (A2); (4) RLS hard boundary tenant_id IN (SELECT user_tenant_ids()) across Ring-0 archetypes + Ring-2 sub_accounts/snapshots/conversations — confirmed BOTH public.user_tenant_ids() (project_rls) and saas_core.user_tenant_ids() (fix_archetype_rls) exist + are identical, so the saas_core.-qualified Ring-2 policies resolve; (5) NO app-owned custom read path bypasses scoping (agency uses only stock SDK plugins — unlike beauty B7's anon public-booking; dashboard/onboarding are hardcoded mock, touch no tenant data). Two-layer model documented: RLS (can't see non-member tenants) + active-tenant filter (narrows to the selected sub-account, cache clears on switch). Doc carries the chain table + A-CHECK smoke checklist + an M-LOCK convention note (standardize public. vs saas_core. user_tenant_ids() form). agency-os code queue complete (A1–A3) → M-AGENCY staged behind A-CHECK. ALL FOUR APPS NOW CODE-COMPLETE → next milestone M-LOCK. Pre-existing uncommitted .env.example + config/app.tsx edits left untouched.
- #17 · M-LOCK · L1 · done (gate EXIT=0, --strict EXIT=0) · sdk 4cc2cf5 · scripts/check-plugin-capability.mjs — capability gate now audits the RLS isolation form. ALL FOUR APPS code-complete (B/P/R/A gated on human checkpoints) → started M-LOCK convention work. The gate reads each plugin's src/migrations/*.sql and classifies the tenant-isolation form into: canonical (`tenant_id IN (SELECT public.user_tenant_ids())` — the form to lock, plugin-tasks), divergent (inline `saas_core.tenant_members WHERE user_id=auth.uid()` — plugin-forms, same drift as resto's restaurant_tables flagged in R-CHECK/#13), deferred (ENABLE ROW LEVEL SECURITY only, no inline CREATE POLICY → relies on project_rls.sql auto-detection — crm/financial/inventory; financial's own comment says so), no-rls (tenant_id table w/ no RLS — a real gap, none found). Added a `rls` column to the classification table (ok/DIV/def/GAP tokens) + an "RLS isolation form" summary block with explicit DIV/def/GAP call-outs naming the plugins. Report-only — never affects exit code (consistent w/ gate's safe default; ratchets via ENFORCED like capability-completeness). Result across 18 plugins: 1 canonical · 1 divergent (plugin-forms) · 3 deferred (crm/financial/inventory) · 0 no-rls · 0 other. This makes DATA-MODEL.md point 4 ("every non-public table has RLS USING (tenant_id IN (SELECT public.user_tenant_ids())); the capability gate will enforce this next") executable — divergence is machine-visible before the lock. Next: L2 (rewrite plugin-forms' 2 migrations + resto restaurant_tables to canonical form; gate should then show 0 divergent).
- #18 · M-LOCK · L2 · done (gate RLS audit 0 divergent EXIT=0; plugin-forms typecheck EXIT=0; resto-saas typecheck EXIT=0) · sdk beb292f + app(resto) 6c77fc5 · sdk: plugins/plugin-forms/src/migrations/{001_frm_base,002_document_archetype}.sql; app: resto-saas/supabase/migrations/20260101000006_project_tables.sql — standardized the divergent inline RLS form `tenant_id IN (SELECT tenant_id FROM saas_core.tenant_members WHERE user_id = auth.uid())` to the canonical `tenant_id IN (SELECT public.user_tenant_ids())` (L1 flagged exactly these). plugin-forms: 24 policies rewritten (frm_templates ×4, frm_documents ×4+×4 [base + post-archetype recreate], frm_document_files ×4+×4, saas_core.documents ×4) — both migration files now 0 tenant_members refs, 12 canonical refs each. Capability gate RLS audit flipped 1 canonical·1 divergent → **2 canonical·0 divergent** (3 deferred crm/financial/inventory unchanged). resto-saas project_tables.sql: all 8 occurrences converted (dynamic FOREACH loop over staff_members/menu_items/payment_methods/orders/order_items ×... + the explicit restaurant_tables block) — whole file canonical; `public.user_tenant_ids()` confirmed defined in resto spine 20260101000002_project_rls.sql. Note: the canonical function DEFINITION itself (saas_core.sql tenant_members policies + project_rls.sql user_tenant_ids() body) legitimately still reads tenant_members — left untouched (that's the source of truth, not a policy divergence). SQL-only changes → no TS impact; both typechecks green. Next: L3 (confirm project_rls.sql auto-detection emits canonical form for the 3 deferred plugins crm/financial/inventory; if so document deferred==canonical-at-apply, else make policies explicit).
- #19 · M-LOCK · L3 · done (gate EXIT=0, 2 canonical · 0 divergent · 3 deferred · 0 no-rls) · sdk (pending commit) · docs/dogfood-sprint/DATA-MODEL.md (point 4 expanded) + scripts/check-plugin-capability.mjs (deferred comment) — deferred-set decision: CONFIRMED `deferred == canonical-at-apply`. Inspected the 3 deferred plugins (crm/financial/inventory): each ships `public.<table>` with `tenant_id uuid NOT NULL REFERENCES saas_core.tenants(id)` + `ALTER TABLE … ENABLE ROW LEVEL SECURITY` and NO `CREATE POLICY` (verified by grep — financial's own migration comment says "RLS via project_rls.sql auto-detection"). Inspected the shared spine `20260101000002_project_rls.sql` (identical across beauty/resto/agency): a DO-block loops `information_schema.columns` for `table_schema='public' AND column_name='tenant_id' AND table_type='BASE TABLE' AND table_name NOT LIKE '\_%'`, ENABLEs RLS, drops the 4 tenant_isolation_* policies, and recreates them as `tenant_id::uuid IN (SELECT public.user_tenant_ids())` for SELECT/INSERT/UPDATE/DELETE. All deferred-plugin tables (pipelines, crm_activities, payment_methods, financial_movements, stock_*, recipes, …) are normal-named public base tables with a tenant_id column → caught by the loop → land on the CANONICAL form at apply. So `deferred` is not a divergence, only a deferral of WHERE the policy text lives (the app's project_rls.sql, not the plugin migration). Safety preconditions for deferral, now documented: table in `public`, is a BASE TABLE, has `tenant_id`, NOT `_`-prefixed. Documented in DATA-MODEL.md point 4 (expanded into a 4-bullet form taxonomy: canonical / deferred==canonical-at-apply / divergent / no-rls, referencing the gate) + a CONFIRMED(L3) note in the gate's `deferred` header comment. Docs/comment-only change (no TS, no SQL behaviour change) → no typecheck needed; gate re-run EXIT=0 unchanged. Next: L4 (lock — gate RLS audit into --strict / ENFORCED-RLS asserting 0 divergent + 0 no-rls, confirm DATA-MODEL pt4 reads as locked convention, hand user_tenant_ids() form to Fayz generation).
- #20 · M-LOCK · L4 · done (gate --strict EXIT=0: 2 canonical · 0 divergent · 3 deferred · 0 no-rls · 0 other; default EXIT=0; neg-test EXIT=1) · sdk ec5d342 · scripts/check-plugin-capability.mjs + docs/dogfood-sprint/DATA-MODEL.md — LOCKED the RLS convention. Flipped the gate's RLS audit from report-only into a hard `--strict` enforcement: a new `RLS_ALLOWED = {canonical, deferred, n/a}` set is checked against EVERY plugin (not just ENFORCED — the lock is universal), and any plugin whose form is `divergent`/`no-rls`/`other` pushes a failure → exit 1. `deferred` stays allowed because L3 confirmed it resolves to the canonical policy at apply (project_rls.sql auto-detect). Also surfaced the previously-unprinted `other` bucket in the audit report (`??` call-out) and updated the header comment block + the strict-pass message ("RLS convention LOCKED: all plugins on canonical/deferred"). Proved the lock bites with a standalone predicate neg-test (rows with divergent+no-rls → 2 would-fail, exit 1; canonical passes). DATA-MODEL.md point 4 rewritten: heading now "One isolation rule, everywhere — LOCKED (M-LOCK / L4)", states the gate ENFORCES under --strict + the current 2·0·3·0·0 state, and the divergent/no-rls/other bullet now reads "hard CI failure". Added a new section "Handed to Fayz generation (the locked emit-target)" pinning what generation emits every time: Ring 0 verbatim + Ring 1 per plugin + Ring 2 per app + canonical `tenant_id IN (SELECT public.user_tenant_ids())` inline RLS on every tenant_id table + the gate as the contract test (generated plugins must pass --strict). **M-LOCK convention lock COMPLETE (L1–L4).** All four apps remain code-complete; the only remaining work is the four HUMAN-CHECKPOINTs (B-CHECK/P3/R-CHECK/A-CHECK) — no unblocked autonomous code task remains. Next iteration: re-scan queues + FEEDBACK; if still all-staged, idle with refreshed FOR THE HUMAN.
- #21 · M-LOCK · idle-scan · all-staged (gate --strict EXIT=0: 2 canonical · 0 divergent · 3 deferred · 0 no-rls · 0 other) · sdk (pending) · docs/dogfood-sprint/STATE.md — re-scanned all four queues + FEEDBACK per #20's instruction. Confirmed every code task ticked (beauty B2–B7, pulse P1–P2, resto R1–R4, agency A1–A3, M-LOCK L1–L4); only the four HUMAN-CHECKPOINTs remain ([~] B-CHECK/P3/R-CHECK/A-CHECK), all live-DB-apply + login-smoke; FEEDBACK empty → no unblocked autonomous code task. Re-ran the locked gate as an integrity check (lock holds, exit 0). Refreshed FOR THE HUMAN with a top ⏸ banner stating the sprint is idle until a checkpoint is run + feedback dropped. No app code touched. Next: idle re-scan until FEEDBACK appears or a checkpoint surfaces follow-up work.
- #22 · M-LOCK · idle-scan · all-staged (gate --strict EXIT=0: 2 canonical · 0 divergent · 3 deferred · 0 no-rls · 0 other) · sdk (pending) · docs/dogfood-sprint/STATE.md — re-scanned all four queues + FEEDBACK per #21. Every code task remains ticked (beauty B2–B7, pulse P1–P2, resto R1–R4, agency A1–A3, M-LOCK L1–L4); only the four HUMAN-CHECKPOINTs ([~] B-CHECK/P3/R-CHECK/A-CHECK) remain, all live-DB-apply + login-smoke; FEEDBACK empty → no unblocked autonomous code task. Re-ran the locked capability gate as an integrity check (lock holds, exit 0). Bumped the FOR THE HUMAN idle banner to iteration #22. No app code touched. Next: idle re-scan until FEEDBACK appears or a checkpoint surfaces follow-up work.
- #23 · M-LOCK · idle-scan · all-staged (gate --strict EXIT=0: 2 canonical · 0 divergent · 3 deferred · 0 no-rls · 0 other) · sdk (pending) · docs/dogfood-sprint/STATE.md — re-scanned all four queues + FEEDBACK per #22. Every code task remains ticked (beauty B2–B7, pulse P1–P2, resto R1–R4, agency A1–A3, M-LOCK L1–L4); only the four HUMAN-CHECKPOINTs ([~] B-CHECK/P3/R-CHECK/A-CHECK) remain, all live-DB-apply + login-smoke; FEEDBACK empty → no unblocked autonomous code task. Re-ran the locked capability gate as an integrity check (lock holds, exit 0; PIPESTATUS-clean re-run confirmed EXIT=0). Bumped the FOR THE HUMAN idle banner to iteration #23. No app code touched. Next: idle re-scan until FEEDBACK appears or a checkpoint surfaces follow-up work.
- #24 · M-LOCK · idle-scan · all-staged (gate --strict EXIT=0: 2 canonical · 0 divergent · 3 deferred · 0 no-rls · 0 other) · sdk (pending) · docs/dogfood-sprint/STATE.md — re-scanned all four queues + FEEDBACK per #23. Every code task remains ticked (beauty B2–B7, pulse P1–P2, resto R1–R4, agency A1–A3, M-LOCK L1–L4); only the four HUMAN-CHECKPOINTs ([~] B-CHECK/P3/R-CHECK/A-CHECK) remain, all live-DB-apply + login-smoke; FEEDBACK empty → no unblocked autonomous code task. Re-ran the locked capability gate as an integrity check (lock holds, exit 0). Bumped the FOR THE HUMAN idle banner to iteration #24. No app code touched. Next: idle re-scan until FEEDBACK appears or a checkpoint surfaces follow-up work.
- #24 · beauty-saas · B-CHECK (MANUAL, human+Claude — not the loop) · B4 APPLIED to live DB via Supabase Management API (sbp token); commission_rate column live (400→200). RLS/tenant isolation PROVEN (svc 44 clients vs test-user 8). B3 onboarding correct on real data (3/4). B5 commission e2e VERIFIED (admin set 30%, test user reads under RLS, 30%×150=45). B6/B7 public-booking REVERTED (af7fd5b). Open: 3 hardcoded metrics, visual smoke, in-app B5 insert.
- #25 · beauty-saas · DASHBOARD FIX (MANUAL, senior debug — not the loop) · 4a54bea · ROOT CAUSE of "0/4 onboarding, 0 clients" in the running app: dashboard counted via `fayz.data.*` = the Fayz PLATFORM runtime API, which has NO backend in standalone `pnpm dev` (beauty env has no VITE_FAYZ_API_BASE_URL) → every count empty. Rest of app reads Supabase directly via getSupabaseClientOptional(). Added src/lib/dashboard-data.ts (countRows/listRows over the same authed Supabase client, same call shape), swapped dashboard.tsx's 5 fayz.data sites. VERIFIED LIVE in browser as test user: onboarding 0/4→3/4, Agendamentos Hoje 0→1, Receita(Semana) —→R$396. typecheck EXIT=0. REMAINING (minor): "Bookings 859" = marketing plugin MOCK (beauty labels conversions as "Bookings"; marketing not wired to real data); "Avaliação 4,9" hardcoded (no ratings table); "Clientes Ativos 0" real (no last_visit in 90d). GENERAL LESSON: any loop-written code using fayz.data is dead in standalone dev — resto/agency custom dashboards likely have the same bug.
- #26 · refactor S1+S2 · order-to-cash: event-sourced money · MANUAL+verified · app f462528 (v_invoice_balances) + sdk b74e41e (financial reads derived) + plan 83828e4 · S1: v_invoice_balances derives amount/paid/balance/status from the append-only financial_movements ledger (fixes live drift: REC-00014 stored 'scheduled' but fully paid -> derived 'paid'; quote-kind receivables counted; cancelled=0 outstanding). S2: financial getInvoices/getInvoiceById read paid/status from the view, not stored metadata.paidAmount/stage. VERIFIED E2E IN BROWSER (flow #5): created receivable REC-00001 (Maria Rosa, Consulta R$99) -> derived 'Aberto' -> paid R$99 via Dinheiro -> derived 'Pago' (DB v_invoice_balances: paid 99, balance 0, status paid). FINDINGS: (a) fresh tenant has no payment_method_types (financial wizard skipped) -> cannot receive until configured; consider default-seed on tenant creation. (b) PaymentModal store didn't refetch types after they were added until modal remount (minor). Next: S3 (operational status only on bookings; v_bookings derives display) — will test the agenda booking flow e2e.
- #27 · S3 + flow#1 e2e · v_bookings operational/financial split + booking flow verified · app 8c2f1f5 · S3: v_bookings already sources operational status from bookings.status (single writer); added derived payment_status/order_paid/order_balance from v_invoice_balances (append-only) — separates operational vs financial lifecycle in the read model. Verified: this tenant's bookings show operational status + null payment_status (not invoiced). FLOW #1 VERIFIED E2E IN BROWSER: Agenda → Criar → inline "Novo cliente" (Cliente Agenda Teste) → persisted persons(kind=customer)+public.clients ext, correct tenant (NO 403 — tenant fallback holds on agenda path too) → professional + Consulta R$99 → Salvar → booking persisted (appointment/scheduled, order_total 99, 1 booking_item + 1 order_item, tenant correct; dual-write consistent). FINDING: 406 on v_leads — agenda contactLookup queries v_leads .single() for a customer (0 rows → 406); non-fatal, sloppy lookup to clean. Next: S4 (order_items single source; booking_items thin link) OR continue remaining flows (#2 quote full, #3 quote→invoice, #4 receive existing).
- #28 · S4 order_items single source · app afbcef0 + sdk 93a663a · booking_items was a full duplicate of order_items (same data, same txn). Backfilled order_items for the 1 order that only had booking_items; repointed v_bookings.services/duration at order_items; agenda hydration now reads order_items (by order_id), create/update write ONLY order_items. booking_items kept (deprecated), no longer read/written. VERIFIED E2E: agenda day view renders bookings + services (Consulta) from order_items, 0 console errors (read path). Write path code-verified (booking_items insert removed) + typecheck EXIT=0. FINDINGS: (a) 1 booking has order_id=NULL (anomalous — a booking should always have its commercial order; data-integrity gap to fix/backfill). (b) stale Vite HMR threw a transient itemsByBookingId ref error until a full (non-hash) reload — testing artifact, not a code bug. Next: S5 (transactional fn_invoice_from_order/fn_pay_invoice) OR validate quote->invoice flow (#2/#3).
- #29 · S5 fn_invoice_from_order + quote→invoice e2e · app 20260617000005 + sdk (crm) · Built public.fn_invoice_from_order(order_id, due_date, status) — SECURITY DEFINER, tenant-guarded (user_tenant_ids), idempotent (skips if a bill movement exists): generates REC ref via next_sequence, sets stage=invoiced/direction=credit, preserves quoteNumber, inserts the bill movement — ALL in one transaction. Wired CRM approveQuote to call it (replaced inline steps 2-4); deal→won + lead→customer cascades stay in JS. VERIFIED E2E IN BROWSER (flows #2+#3): created quote Q-0002 (Maria Rosa, Consulta R$99) → Sent → Approved (confirm) → order promoted to REC-00004, stage=invoiced, direction=credit, 1 bill movement, v_invoice_balances flow=receivable status=open. Atomic + derived. FINDING: v_leads 406 still fires (contactLookup .single() on a customer) — flagged, non-fatal. Next: S6 (lifecycle CHECK) and/or adopt fn_invoice_from_order in agenda bridge + financial createInvoice.
- #30 · S6 order lifecycle CHECK · app 20260617000006 · Added saas_core.orders CHECK orders_kind_status_valid: per-kind allowed status (quote: draft/sent/approved/rejected/expired/converted; deal: open/won/lost; appointment + service_order: scheduled/confirmed/in_progress/completed/cancelled/no_show/invoiced/paid; invoice_receivable/payable: open/paid/partial/overdue/cancelled; ELSE true for unknown kinds). NOT VALID (legacy messy rows skipped; enforces new INSERT/UPDATE). status IS NULL allowed. Logic validated non-destructively (quote/paid→reject, quote/banana→reject, deal/paid→reject, valid ones pass, unknown kind passes). Confirmed no current plugin write violates it. ORDER-TO-CASH REFACTOR S1–S6 COMPLETE. LATENT NOTE for S5-adoption: fn_invoice_from_order defaults p_status='approved' (valid for quote); if agenda/financial adopt it on appointment/invoice kinds they must pass a kind-valid status (e.g. 'open'/'invoiced') or the CHECK rejects. Next: complete fn_invoice_from_order adoption in agenda+financial, fn_pay_invoice, + cleanup track (v_leads 406, PaymentModal disclaimer, order_id-null booking).
